<style>
  /* ============================================
     Rogue Night — Digital Health Check Report (v3)
     Brand-locked. Phase 1 tokens only.
     ============================================ */
  :root {
    --ink: #0A0E1A;
    --obsidian: #050608;
    --gold: #C9A961;
    --gold-soft: rgba(201, 169, 97, 0.16);
    --gold-line: rgba(201, 169, 97, 0.32);
    --ember: #C2410C;
    --ember-soft: rgba(194, 65, 12, 0.10);
    --parchment: #EDE8DD;
    --parchment-deep: #E2DCCD;
    --slate: #6B7280;
    --slate-soft: #9CA3AF;
    --rule: rgba(10, 14, 26, 0.10);
    --rule-strong: rgba(10, 14, 26, 0.22);
    --hi: #2F855A;
    --hi-soft: rgba(47, 133, 90, 0.10);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }
  html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
  body {
    font-family: 'Source Serif 4', 'Instrument Serif', Georgia, serif;
    font-size: 16px;
    line-height: 1.65;
    color: var(--ink);
    background: var(--parchment);
  }

  /* ---------- Layout ---------- */
  .page { max-width: 880px; margin: 0 auto; padding: 0 56px; }
  section.block { padding: 96px 0; border-top: 1px solid var(--rule); }
  section.block:first-of-type { border-top: 0; }

  /* ---------- Typography ---------- */
  .display, h1, h2, h3 {
    font-family: 'Instrument Serif', Georgia, serif;
    font-weight: 400;
    letter-spacing: -0.01em;
  }
  h1 { font-size: 64px; line-height: 1.04; letter-spacing: -0.02em; }
  h2 { font-size: 38px; line-height: 1.12; letter-spacing: -0.015em; }
  h3 { font-size: 22px; line-height: 1.25; }
  .italic { font-style: italic; }
  .eyebrow {
    font-family: 'Inter', sans-serif;
    font-size: 11px; font-weight: 600;
    letter-spacing: 0.18em; text-transform: uppercase;
    color: var(--gold);
  }
  .meta {
    font-family: 'JetBrains Mono', ui-monospace, monospace;
    font-size: 11.5px; letter-spacing: 0.04em; color: var(--slate);
  }
  .body-lede {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; line-height: 1.45;
    color: var(--ink); font-style: italic;
  }
  p { margin-bottom: 14px; }
  p.tight { margin-bottom: 8px; }
  strong { font-weight: 600; }
  em { font-style: italic; }

  /* ---------- Cover ---------- */
  .cover { background: var(--ink); color: var(--parchment); padding: 0; border-top: 0; }
  .cover-inner {
    max-width: 880px; margin: 0 auto;
    padding: 96px 56px 88px; min-height: 720px;
    display: flex; flex-direction: column; justify-content: space-between;
  }
  .cover-top { display: flex; align-items: center; gap: 14px; }
  .cover-mark { width: 52px; height: 52px; }
  .cover-wordmark {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; font-style: italic;
    letter-spacing: 0.01em; color: var(--parchment);
  }
  .cover-title-block { margin-top: 64px; }
  .cover-eyebrow {
    font-family: 'Inter', sans-serif;
    font-size: 12px; font-weight: 600;
    letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 24px;
  }
  .cover-title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 76px; line-height: 1.02;
    letter-spacing: -0.025em; color: var(--parchment);
    font-weight: 400;
  }
  .cover-title .accent { font-style: italic; color: var(--gold); }
  .cover-subtitle {
    margin-top: 28px;
    font-family: 'Source Serif 4', 'Instrument Serif', Georgia, serif;
    font-size: 18px; line-height: 1.55;
    color: rgba(237, 232, 221, 0.78); max-width: 580px;
  }
  .cover-bottom {
    display: flex; justify-content: space-between; align-items: end;
    gap: 32px; padding-top: 48px;
    border-top: 1px solid rgba(201, 169, 97, 0.22);
  }
  .cover-meta-block { display: flex; flex-direction: column; gap: 4px; }
  .cover-meta-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.18em;
    text-transform: uppercase; color: rgba(201, 169, 97, 0.7);
  }
  .cover-meta-value {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 17px; color: var(--parchment); font-style: italic;
  }

  /* ---------- Section header ---------- */
  .section-head { margin-bottom: 48px; }
  .section-head .number {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 14px;
    color: var(--gold); margin-bottom: 12px; letter-spacing: 0.04em;
  }
  .section-head h2 { max-width: 720px; }
  .section-head .lede {
    margin-top: 18px;
    font-family: 'Source Serif 4', 'Instrument Serif', Georgia, serif;
    font-size: 17px; line-height: 1.6;
    color: var(--slate); max-width: 640px;
  }

  /* ---------- Snapshot card ---------- */
  .snapshot {
    background: white; border: 1px solid var(--rule); border-radius: 4px;
    padding: 36px 40px;
    display: grid; grid-template-columns: 1fr 1fr;
    column-gap: 48px; row-gap: 18px;
  }
  .snapshot-row { display: flex; flex-direction: column; gap: 4px; page-break-inside: avoid; }
  .snapshot-row.full-width {
    grid-column: 1 / -1;
  }
  .snapshot-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--slate);
  }
  .snapshot-value {
    font-family: 'Inter', sans-serif;
    font-size: 16px; color: var(--ink); line-height: 1.5;
  }
  .snapshot-value.serif {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 19px; font-style: italic;
  }

  /* ---------- Key Benefits boxes ---------- */
  .benefits { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; margin-top: 36px; }
  .benefit { background: var(--gold-soft); border-left: 2px solid var(--gold); padding: 22px 22px 26px; min-width: 0; overflow-wrap: break-word; word-wrap: break-word; }
  .benefit-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold); margin-bottom: 10px;
  }
  .benefit-title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; line-height: 1.18;
    color: var(--ink); margin-bottom: 10px; letter-spacing: -0.01em;
  }
  .benefit-body { font-size: 14.5px; line-height: 1.5; color: var(--ink); text-align: justify; }

  /* ---------- Quantified benefits table ---------- */
  .qb-table { width: 100%; border-collapse: collapse; margin-top: 8px; }
  .qb-table th {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate);
    text-align: left; padding: 12px 0;
    border-bottom: 1px solid var(--rule-strong); font-weight: 500;
  }
  .qb-table th.right { text-align: right; }
  .qb-table td {
    padding: 16px 0;
    border-bottom: 1px solid var(--rule);
    font-size: 15.5px; vertical-align: top;
  }
  .qb-table td.right { text-align: right; font-variant-numeric: tabular-nums; }
  .qb-table .change-cell {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 19px; line-height: 1.3;
  }
  .qb-table .change-cell .sub {
    font-family: 'Inter', sans-serif;
    font-size: 13px; color: var(--slate);
    display: block; margin-top: 3px; font-style: normal;
  }
  .qb-table .num {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; color: var(--ink);
  }
  .qb-table tr.subtotal td {
    border-top: 2px solid var(--ink); border-bottom: 0;
    padding-top: 22px; padding-bottom: 6px;
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; font-style: italic;
  }
  .qb-table tr.subtotal td.right { font-style: italic; }
  .qb-totals {
    margin-top: 32px;
    background: rgba(201, 169, 97, 0.10);
    border: 1px solid var(--gold-line);
    border-left: 3px solid var(--gold);
    color: var(--ink);
    padding: 32px 36px;
    display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 32px;
  }
  .qb-total { display: flex; flex-direction: column; gap: 6px; }
  .qb-total .label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold);
  }
  .qb-total .value {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 36px; line-height: 1.05;
    letter-spacing: -0.015em; color: var(--ink);
  }
  .qb-total .value .unit {
    font-size: 14px; color: var(--slate);
    font-style: italic; margin-left: 4px;
  }
  .qb-total .note {
    font-size: 13px; color: var(--slate);
    line-height: 1.5; margin-top: 4px;
  }
  .qb-assumptions {
    margin-top: 28px; padding: 18px 22px;
    background: var(--parchment-deep); border-left: 2px solid var(--slate);
    font-size: 13.5px; line-height: 1.6; color: var(--slate);
  }
  .qb-assumptions strong { color: var(--ink); font-weight: 600; }

  /* ---------- Stack category ---------- */
  .stack-category { margin-top: 56px; }
  .stack-category:first-of-type { margin-top: 0; }
  .stack-category-head {
    display: flex; align-items: baseline; gap: 16px;
    margin-bottom: 18px; padding-bottom: 12px;
    border-bottom: 1px solid var(--rule);
  }
  .stack-category-num {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 14px; color: var(--gold);
  }
  .stack-category-title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 26px; color: var(--ink); letter-spacing: -0.01em;
  }

  /* ---------- Tool card ---------- */
  .tool { padding: 28px 0; border-bottom: 1px solid var(--rule); }
  .tool:last-child { border-bottom: 0; }
  .tool-top {
    display: grid; grid-template-columns: 1fr auto;
    gap: 24px; align-items: start; margin-bottom: 18px;
  }
  .tool-name {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 30px; line-height: 1.05;
    color: var(--ink); letter-spacing: -0.015em; margin-bottom: 6px;
  }
  .tool-subtitle {
    font-family: 'Inter', sans-serif;
    font-size: 14.5px; color: var(--slate);
  }
  .tool-badges { display: flex; flex-direction: column; align-items: flex-end; gap: 6px; }
  .badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; letter-spacing: 0.12em;
    text-transform: uppercase; padding: 4px 10px;
    border-radius: 2px; border: 1px solid; white-space: nowrap;
  }
  .badge.priority-high { color: white; background: var(--ember); border-color: var(--ember); }
  .badge.priority-med { color: var(--ink); background: var(--gold); border-color: var(--gold); }
  .badge.priority-low { color: var(--slate); background: white; border-color: var(--rule-strong); }
  .badge.when { color: var(--ink); background: var(--gold-soft); border-color: var(--gold-line); }
  .badge.existing { color: white; background: var(--ink); border-color: var(--ink); }
  .existing-callout {
    font-family: 'Source Serif 4', serif; font-size: 14.5px; color: var(--ink);
    background: rgba(201, 169, 97, 0.10); border-left: 3px solid var(--gold);
    padding: 10px 14px; margin-bottom: 18px; border-radius: 0 4px 4px 0;
  }
  .tool-grid {
    display: grid; grid-template-columns: 1.6fr 1fr;
    gap: 36px; margin-top: 4px;
  }
  .tool-rationale { display: flex; flex-direction: column; gap: 14px; }
  .rationale-block { display: flex; flex-direction: column; gap: 4px; }
  .rationale-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate);
  }
  .rationale-text {
    font-family: 'Source Serif 4', 'Instrument Serif', Georgia, serif;
    font-size: 15.5px; line-height: 1.55; color: var(--ink);
  }
  .rationale-text.warn { color: var(--ember); }
  .rationale-text em { font-style: italic; }
  .tool-pricing {
    background: var(--parchment-deep); padding: 18px 20px;
    border-radius: 2px; align-self: start;
  }
  .tool-pricing-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate); margin-bottom: 10px;
  }
  .tier-row {
    display: flex; justify-content: space-between; align-items: baseline;
    padding: 6px 0; border-bottom: 1px solid rgba(10, 14, 26, 0.06);
    font-size: 14px;
  }
  .tier-row:last-child { border-bottom: 0; }
  .tier-row.recommended {
    background: var(--gold-soft); margin: 0 -12px;
    padding: 8px 12px; border-radius: 2px; border-bottom: 0;
  }
  .tier-name {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 16px; color: var(--ink);
  }
  .tier-row.recommended .tier-name::after {
    content: ' · recommended';
    font-family: 'Inter', sans-serif;
    font-size: 11px; font-weight: 600; letter-spacing: 0.06em;
    text-transform: uppercase; color: var(--gold); font-style: normal;
  }
  .tier-price {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px; color: var(--ink); font-variant-numeric: tabular-nums;
  }
  .upgrade-trigger {
    margin-top: 12px; font-size: 12.5px;
    line-height: 1.5; color: var(--slate); font-style: italic;
  }
  .upgrade-trigger strong { color: var(--ink); font-style: normal; }
  .integration-callout {
    margin-top: 16px; padding-top: 14px;
    border-top: 1px solid var(--rule);
    font-size: 13.5px; color: var(--slate); line-height: 1.55;
  }
  .integration-callout strong { color: var(--ink); }
  .integration-callout .native { color: var(--hi); font-weight: 600; }
  .integration-callout .glue { color: var(--ember); font-weight: 600; }

  /* ---------- Stack at a glance ---------- */
  .glance-priority { margin-top: 8px; margin-bottom: 56px; }
  .glance-row {
    display: grid; grid-template-columns: 140px 1fr;
    gap: 32px; padding: 24px 0;
    border-bottom: 1px solid var(--rule); align-items: start;
  }
  .glance-priority-label {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 24px; line-height: 1.1;
    color: var(--ink); letter-spacing: -0.01em;
  }
  .glance-priority-label .when {
    display: block;
    font-family: 'Inter', sans-serif;
    font-size: 12px; font-weight: 600; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--gold);
    margin-top: 6px; font-style: normal;
  }
  .glance-tools { display: flex; flex-wrap: wrap; gap: 8px; }
  .glance-pill {
    background: white; border: 1px solid var(--rule-strong);
    padding: 8px 14px; border-radius: 2px;
    display: flex; flex-direction: column; gap: 2px;
  }
  .glance-pill .name {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 17px; color: var(--ink);
  }
  .glance-pill .role {
    font-family: 'Inter', sans-serif;
    font-size: 12px; color: var(--slate);
  }

  /* ---------- Integration map (v3 — named flows) ---------- */
  .integration-map {
    margin-top: 8px; padding: 36px 40px;
    background: white; border: 1px solid var(--rule); border-radius: 4px;
  }
  .integration-map-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate); margin-bottom: 28px;
  }
  .map-flow-block { margin-bottom: 32px; }
  .map-flow-block:last-child { margin-bottom: 0; }
  .map-flow-name {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 19px; line-height: 1.25;
    color: var(--ink); margin-bottom: 14px;
    letter-spacing: -0.01em; padding-bottom: 8px;
    border-bottom: 1px solid var(--gold-line);
  }
  .map-flow-name .num {
    font-style: italic; color: var(--gold); margin-right: 8px;
  }
  .map-flow-name .helper {
    display: block; font-family: 'Inter', sans-serif;
    font-size: 13px; color: var(--slate);
    margin-top: 4px; font-weight: 400;
  }
  .map-row {
    display: flex; align-items: center; gap: 16px; flex-wrap: nowrap;
  }
  .map-node {
    background: var(--parchment-deep); border: 1.5px solid var(--ink);
    padding: 12px 18px; border-radius: 2px;
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 17px; color: var(--ink);
    min-width: 120px; text-align: center; line-height: 1.2;
  }
  .map-node.center { background: var(--ink); color: var(--parchment); }
  .map-node .small {
    display: block;
    font-family: 'Inter', sans-serif;
    font-size: 11px; color: var(--slate);
    margin-top: 3px; font-style: normal;
  }
  .map-node.center .small { color: rgba(237, 232, 221, 0.6); }
  .map-arrow {
    display: flex; flex-direction: row;
    align-items: center; gap: 6px;
    flex: 1 1 auto; min-width: 130px;
  }
  .map-arrow .line {
    flex: 1 1 0; height: 1px;
    background: var(--ink); min-width: 14px;
  }
  .map-arrow .action {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 13px;
    color: var(--gold); text-align: center;
    line-height: 1.2; white-space: nowrap;
    padding: 0 2px;
  }
  .map-arrow .line.tail {
    position: relative;
  }
  .map-arrow .line.tail::after {
    content: '▸'; position: absolute;
    right: -4px; top: -10px;
    color: var(--ink); font-size: 12px;
  }

  /* ---------- Phased rollout ---------- */
  .phases { margin-top: 8px; }
  .phase {
    display: grid; grid-template-columns: 196px 1fr;
    gap: 36px; padding: 32px 0;
    border-bottom: 1px solid var(--rule);
  }
  .phase:last-child { border-bottom: 0; }
  .phase-marker { display: flex; flex-direction: column; gap: 4px; }
  .phase-week {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 30px; line-height: 1;
    color: var(--gold); letter-spacing: -0.01em;
  }
  .phase-week .num { color: var(--ink); font-style: normal; }
  .phase-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate); margin-top: 6px;
  }
  .phase-content { display: flex; flex-direction: column; gap: 14px; }
  .phase-headline {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; line-height: 1.22;
    color: var(--ink); letter-spacing: -0.01em;
  }
  .phase-tasks { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .phase-tasks li {
    font-family: 'Source Serif 4', 'Instrument Serif', Georgia, serif;
    font-size: 15.5px; line-height: 1.55;
    padding-left: 22px; position: relative; color: var(--ink);
  }
  .phase-tasks li::before {
    content: ''; position: absolute; left: 0; top: 11px;
    width: 10px; height: 1px; background: var(--gold);
  }

  /* ---------- Cost ---------- */
  .cost-table { width: 100%; border-collapse: collapse; margin-top: 8px; }
  .cost-table th {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--slate);
    text-align: left; padding: 12px 0;
    border-bottom: 1px solid var(--rule-strong); font-weight: 500;
  }
  .cost-table th.right { text-align: right; }
  .cost-table td {
    padding: 14px 0; border-bottom: 1px solid var(--rule);
    font-size: 15.5px; vertical-align: top;
  }
  .cost-table td.right { text-align: right; font-variant-numeric: tabular-nums; }
  .cost-table tr.total td {
    border-bottom: 0; border-top: 2px solid var(--ink);
    padding-top: 18px;
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 22px; font-style: italic;
  }
  .cost-table .tool-cell {
    font-family: 'Instrument Serif', Georgia, serif; font-size: 18px;
  }
  .cost-table .tool-cell .sub {
    font-family: 'Inter', sans-serif;
    font-size: 13px; color: var(--slate);
    display: block; font-style: normal; margin-top: 2px;
  }
  .rn-implement {
    margin-top: 36px; padding: 32px 36px;
    background: var(--ink); color: var(--parchment);
    border-left: 3px solid var(--gold);
  }
  .rn-implement-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--gold); margin-bottom: 14px;
  }
  .rn-implement h3 {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 28px; line-height: 1.2;
    color: var(--parchment); margin-bottom: 14px; letter-spacing: -0.01em;
  }
  .rn-implement p {
    font-size: 15.5px; line-height: 1.55;
    color: rgba(237, 232, 221, 0.85); margin-bottom: 12px;
  }
  .rn-implement ul {
    padding-left: 22px; margin: 8px 0 16px;
    color: rgba(237, 232, 221, 0.85);
  }
  .rn-implement li { font-size: 15px; line-height: 1.55; margin-bottom: 6px; }
  .rn-implement .what-we-dont {
    margin-top: 18px; padding: 14px 18px;
    background: rgba(237, 232, 221, 0.05);
    border-left: 2px solid var(--ember);
  }
  .rn-implement .what-we-dont strong { color: var(--ember); }
  .rn-implement .quote-line {
    margin-top: 20px; padding-top: 18px;
    border-top: 1px solid rgba(201, 169, 97, 0.32);
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 18px; font-style: italic; color: var(--gold);
  }

  /* ---------- Future digital employees ---------- */
  .batch { margin-top: 56px; }
  .batch:first-of-type { margin-top: 8px; }
  .batch-head {
    display: flex; align-items: baseline; gap: 16px;
    margin-bottom: 22px; padding-bottom: 12px;
    border-bottom: 1px solid var(--rule);
  }
  .batch-when {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 14px; color: var(--gold);
  }
  .batch-title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 26px; color: var(--ink); letter-spacing: -0.01em;
  }
  .batch-totals {
    margin-left: auto;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; letter-spacing: 0.10em;
    text-transform: uppercase; color: var(--slate); text-align: right;
  }
  .batch-totals strong { color: var(--ember); font-weight: 600; }
  .agent {
    padding: 22px 0; border-bottom: 1px solid var(--rule);
    display: grid; grid-template-columns: 1fr 2.6fr 1fr;
    gap: 28px; align-items: start;
  }
  .agent:last-child { border-bottom: 0; }
  .agent-name { display: flex; flex-direction: column; gap: 6px; }
  .agent-name .role {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--gold);
  }
  .agent-name .title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 24px; line-height: 1.05;
    color: var(--ink); letter-spacing: -0.015em;
  }
  .agent-content { display: flex; flex-direction: column; gap: 8px; }
  .agent-content .what { font-size: 15px; line-height: 1.55; color: var(--ink); }
  .agent-content .replaces {
    font-size: 13.5px; line-height: 1.55;
    color: var(--slate); font-style: italic;
  }
  .agent-savings {
    display: flex; flex-direction: column; gap: 10px;
    padding: 14px 16px;
    background: var(--gold-soft); border-left: 2px solid var(--gold);
  }
  .saving-row { display: flex; flex-direction: column; gap: 2px; }
  .saving-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--slate);
  }
  .saving-value {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 19px; color: var(--ink);
    line-height: 1.1; font-style: italic;
  }
  .saving-value .unit { font-size: 12px; color: var(--slate); margin-left: 3px; font-style: normal; }

  /* ---------- Next steps ---------- */
  .next-steps { margin-top: 8px; display: flex; flex-direction: column; gap: 0; }
  .step {
    display: grid; grid-template-columns: 64px 1fr;
    gap: 24px; padding: 28px 0;
    border-bottom: 1px solid var(--rule);
  }
  .step:last-child { border-bottom: 0; }
  .step-num {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 36px; line-height: 1; color: var(--gold);
  }
  .step-content { display: flex; flex-direction: column; gap: 8px; }
  .step-title {
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 24px; line-height: 1.18;
    color: var(--ink); letter-spacing: -0.01em;
  }
  .step-body { font-size: 15.5px; line-height: 1.55; color: var(--ink); }
  .step-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11.5px; color: var(--slate); margin-top: 4px;
  }

  /* ---------- Footer ---------- */
  .doc-footer {
    margin-top: 0; padding: 56px 0 64px;
    border-top: 1px solid var(--rule-strong); text-align: center;
  }
  .doc-footer .mark { width: 36px; height: 36px; margin: 0 auto 16px; opacity: 0.65; }
  .doc-footer p { font-size: 14px; color: var(--slate); margin-bottom: 4px; }
  .doc-footer .legal {
    margin-top: 12px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; color: var(--slate-soft); letter-spacing: 0.06em;
  }
  .doc-footer .tagline {
    font-family: 'Instrument Serif', Georgia, serif;
    font-style: italic; font-size: 16px; color: var(--ink);
  }

  /* ---------- Editor cheat sheet ---------- */
  .editor-cheat {
    background: var(--ember-soft); border: 1px dashed var(--ember);
    padding: 32px 36px; margin: 64px auto 96px; max-width: 880px;
  }
  .editor-cheat .ec-badge {
    display: inline-block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--ember);
    background: white; padding: 4px 10px;
    border: 1px solid var(--ember); border-radius: 2px; margin-bottom: 18px;
  }
  .editor-cheat h3 { font-size: 24px; margin-bottom: 12px; color: var(--ink); }
  .editor-cheat p { font-size: 15px; color: var(--ink); margin-bottom: 12px; }
  .editor-cheat ul { padding-left: 22px; margin-bottom: 12px; }
  .editor-cheat li { font-size: 14.5px; line-height: 1.6; margin-bottom: 6px; }
  .editor-cheat code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px; background: white;
    padding: 1px 6px; border: 1px solid var(--rule-strong); border-radius: 2px;
  }

  /* ---------- Page counter (WeasyPrint) ---------- */
  @page {
    @bottom-center {
      content: counter(page);
      font-family: 'Inter', sans-serif;
      font-size: 10px;
      color: #9CA3AF;
      letter-spacing: 0.06em;
    }
  }
  @page :first {
    @bottom-center { content: none; }
  }

  /* ---------- Print ---------- */
  @media print {
    @page { size: A4; margin: 18mm 16mm; }
    body { background: white; font-size: 11pt; }
    .page { padding: 0; max-width: none; }
    section.block { padding: 28pt 0; border-top-color: rgba(10, 14, 26, 0.18); }
    .cover { background: var(--ink) !important; -webkit-print-color-adjust: exact; print-color-adjust: exact; page-break-after: always; }
    .cover-inner { min-height: 240mm; padding: 24mm 16mm; }
    h1, h2, h3, .display { page-break-after: avoid; }
    .tool, .phase, .agent, .step, .glance-row, .map-flow-block { page-break-inside: avoid; }
    .benefits { gap: 12px; }
    .benefits { grid-template-columns: 1fr; gap: 14px; }
    .benefit { min-width: 0; overflow-wrap: break-word; word-wrap: break-word; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .benefit-title { font-size: 20px; }
    .benefit-body { font-size: 13px; }
    .stack-category { page-break-before: auto; }
    .rn-implement { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .editor-cheat { display: none; }
    .doc-footer { page-break-before: avoid; }
  }

  /* ---------- Responsive ---------- */
  @media (max-width: 720px) {
    .page { padding: 0 24px; }
    section.block { padding: 64px 0; }
    h1, .cover-title { font-size: 44px; }
    h2 { font-size: 28px; }
    .snapshot { grid-template-columns: 1fr; }
    .benefits { grid-template-columns: 1fr; }
    .qb-totals { grid-template-columns: 1fr; gap: 18px; }
    .tool-grid { grid-template-columns: 1fr; gap: 18px; }
    .phase, .glance-row { grid-template-columns: 1fr; gap: 12px; }
    .agent { grid-template-columns: 1fr; gap: 12px; }
    .cover-inner { padding: 56px 24px; min-height: auto; }
    .cover-title { font-size: 48px; }
    .integration-map { padding: 24px; }
    .map-row { flex-direction: column; align-items: flex-start; gap: 8px; }
    .map-arrow { flex: 0 0 auto; min-width: auto; gap: 8px; padding: 4px 0; }
    .map-arrow .line { flex: 0 0 24px; min-width: 24px; }
    .map-arrow .action { font-size: 12px; }
  }
</style>
<style>
.ha-img-placeholder{display:flex;align-items:center;justify-content:center;flex-direction:column;gap:6px;background:#f4f4f5;border:1px dashed #d4d4d8;border-radius:8px;color:#71717a;font-size:12px;font-family:system-ui,sans-serif;min-height:80px;padding:16px;box-sizing:border-box;animation:ha-img-pulse 1.5s ease-in-out infinite}
.ha-img-placeholder.ha-failed{animation:none;opacity:.7}
@keyframes ha-img-pulse{0%,100%{opacity:1}50%{opacity:.5}}
@media(prefers-color-scheme:dark){.ha-img-placeholder{background:#27272a;border-color:#3f3f46;color:#a1a1aa}}
</style>
